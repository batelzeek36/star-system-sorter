/**
 * Scorer Module
 * Public API for star system classification
 */

// Export scorer functions here as they are created
// Example: export { classify } from './classify';
// Example: export type { ScorerResult, HDExtract } from './types';
