/**
 * Bridge Module
 * Public API for React Native ↔ Flutter communication
 */

// Export bridge functions here as they are created
// Example: export { GameBridge } from './GameBridge';
// Example: export type { GameCommand, GameEvent } from './types';
