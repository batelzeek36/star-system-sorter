pluginManagement { includeBuild("../node_modules/@react-native/gradle-plugin") }
plugins { id("com.facebook.react.settings") }
extensions.configure(com.facebook.react.ReactSettingsExtension){ ex -> ex.autolinkLibrariesFromCommand() }

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.PREFER_SETTINGS)
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.name = 'S3App'
include ':app'
includeBuild('../node_modules/@react-native/gradle-plugin')

// Flutter module integration (DISABLED - using AAR instead)
// Direct project inclusion causes "Project.afterEvaluate" error
// Use prebuilt AAR method instead (see FLUTTER_AAR_INTEGRATION.md)
// setBinding(new Binding([gradle: this]))
// evaluate(new File(
//   settingsDir.parentFile,
//   'runner_game/.android/include_flutter.groovy'
// ))
